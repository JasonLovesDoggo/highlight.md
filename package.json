{
  "name": "obsidian-highlight-mcp",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "description": "Highlight exact text in Obsidian notes, manually or through Claude and Codex MCP tools.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node esbuild.config.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "^2.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "obsidian": "^1.13.1"
  }
}
